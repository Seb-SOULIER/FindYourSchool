{% extends 'base.html.twig' %}

{% block title %}Trouve ton Ecole{% endblock %}

{% block body %}
<div class="container-fluid vh-100">
    <div class="row page">
        <div class="d-flex flex-column col-lg-4 col-12 menu-rigth">
            <div class="d-flex flex-column text-center justify-content-center align-items-center h-75">
                <div class="d-flex flex-column carteTitle w-75">
                    <h1 class="title">Trouve ton école</h1>
                </div>
            </div>
            <a class="btn btn-secondary btn-css w-50 mx-auto mt-0" href="{{ path('app_student') }}">Entrer</a>
        </div>
        <div class="d-flex col-8 menu-left">
            <div class="d-flex flex-column col-11 m-auto">
                <img class="image" src="{{ asset('build/images/banner-rentree.jpg') }}" alt="Ecoles"/>
                <div>
                    <h2 class="text-light">Bienvenue sur le site ! </h2>
                    <p class="text-light font-small">
                        Vous déménagez! Quelles écoles seront les plus proches? <br>
                        Envie de tous savoir sur ces écoles.
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
